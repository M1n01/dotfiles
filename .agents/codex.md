# Codex-specific Instructions

- Codex loads applicable `AGENTS.override.md` and `AGENTS.md` files
  automatically. Read `CLAUDE.md` only when the repository explicitly treats it
  as shared project guidance.
